/*
  At the time of writing these two (X_AXIS and Y_AXIS) are the same,
  I am keeping them as stand alone variables to allow for easy customisation
  in the future if required.
*/
const X_AXIS = 75;
const Y_AXIS = 75;

export const PX_SWIPED_THRESHOLD = {
  X_AXIS,
  Y_AXIS
};
